{
  "devDependencies": {
    "cypress": "^16.0.0"
  },
  "name": "projectparkee",
  "version": "1.0.0",
  "scripts": {
    "test": "cypress run",
    "test:api": "cypress run --spec cypress/e2e/one-piece-api.cy.js",
    "test:functional": "cypress run --spec cypress/e2e/saucedemo.cy.js",
    "cy:open": "cypress open"
  }
}
